export declare class Util {
    static readRecursive(directory: string, files?: string[]): string[];
    static has(o: any, k: any): boolean;
    static isObject(obj: any): boolean;
    static merge(defaults: any, given: any): any;
    static isClass(input: any): boolean;
}
